<resources>

    <!-- Base application theme. -->
    <style name="AppTheme.Green" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">#009788</item>
        <item name="colorPrimaryDark">#00796a</item>
        <item name="colorAccent">#ff3d00</item>
        <item name="android:windowDisablePreview">true</item>

        <item name="preferenceTheme">@style/PreferenceThemeOverlay</item>

    </style>

    <!-- Base application theme 2. -->
    <style name="AppTheme.Red" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">#f44333</item>
        <item name="colorPrimaryDark">#b90008</item>
        <item name="colorAccent">#2979ff</item>
        <item name="android:windowDisablePreview">true</item>

        <item name="preferenceTheme">@style/PreferenceThemeOverlay</item>

    </style>

    <!-- Base application theme 3. -->
    <style name="AppTheme.Blue" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">#2196f3</item>
        <item name="colorPrimaryDark">#1769aa</item>
        <item name="colorAccent">#ff1744</item>
        <item name="android:windowDisablePreview">true</item>

        <item name="preferenceTheme">@style/PreferenceThemeOverlay</item>

    </style>

    <!-- ToolBar -->
    <style name="ToolBarStyle" parent="Theme.AppCompat">
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:textColorSecondary">@android:color/white</item>
        <item name="actionMenuTextColor">@android:color/black</item>
    </style>

    <style name="AppTheme.Green.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />




</resources>
