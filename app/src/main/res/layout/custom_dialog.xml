<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/ll_dialog"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical"
    android:padding="10dp" >
    <TextView
        android:id="@+id/number_dialog_tv"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAppearance="?android:attr/textAppearanceMedium"
        android:contentDescription="@string/content_description_dialog_number_type"
        android:layout_marginBottom="@dimen/dialog_text_margin_bottom" />
    <EditText
        android:id="@+id/number_dialog_et"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="@string/number_picker_edit_text_hint"
        android:inputType="number"
        >
        <requestFocus />
    </EditText>
</LinearLayout>